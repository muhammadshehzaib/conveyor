# Tells Grafana to load any dashboard JSON files from /var/lib/grafana/dashboards.
apiVersion: 1

providers:
  - name: Conveyor
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /var/lib/grafana/dashboards
